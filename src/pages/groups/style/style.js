import { Box, styled } from '@mui/material';

export const GroupContainer = styled(Box)(() => ({
	height: '100%',
	width: '100%',
	display: 'flex',
	alignItems: 'center',
	flexDirection: 'column',
	padding: 16,
}));
